import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'handson-ionic-framework',
  webDir: 'www'
};

export default config;
